import { describe } from 'jest-circus';

describe('Random Questions (Test A)', () => {
  it('Scenario: User enters challenge mode, expect: random assortment of questions', () => {});
});

describe('Continue after incorrect answer (Test B)', () => {
  it('Scenario: User enters incorrect answer and then presses the next button, expect: directed to next question', () => {});
});
