import { describe } from 'jest-circus';

describe('View past attempts (Test A)', () => {
  it('Scenario: User accesses past attempts page after having completed an exercise, expect: page to display correct information for attempts made', () => {});
});

describe('Viewing others past attempts (Test B)', () => {
  it('Scenario: User tries to view another users past attempts page, expect: error message, no redirection', () => {});
});
