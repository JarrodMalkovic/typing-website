import { describe } from 'jest-circus';

describe('User login (Test F)', () => {
  it('Scenario: User enters invalid credentials, expect: error message, database unchanged', () => {});
});

describe('User logout (Test G)', () => {
  it('Scenario: User presses lougout button, expect: redirected to sign-in page', () => {});
});
