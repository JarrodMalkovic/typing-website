import { describe } from 'jest-circus';

describe('Display keyboard (Test A)', () => {
  it('Scenario: User clicks keyboard button during typing exercise, expect: keyboard popup', () => {});
});

describe('Hide keyboard (Test B)', () => {
  it('Scenario: User clicks keyboard button during typing exercise with keyboard open, expect: keyboard popup removed', () => {});
});
