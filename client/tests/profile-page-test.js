import { describe } from 'jest-circus';

describe('Profile page privacy (Test A)', () => {
  it('Scenario: User tries to access another uses profile page, expect: error message, no redirection', () => {});
});

describe('Updating profile page (Test B)', () => {
  it('Scenario: User tries to update profile information, expect: change to database reflecting profile change', () => {});
});

describe('Updating others profile page (Test C)', () => {
  it('Scenario: User tries to update another users profile page, expect: error message, database unchanged', () => {});
});
