import { describe } from 'jest-circus';

describe('User score (Test A)', () => {
  it('Scenario: User completes exercises with set accuracy and WPM, expect: a score according to those values', () => {});
});

describe('Leaderboard display (Test B)', () => {
  it('Scenario: User navigates to leaderboard, expect: a table of users and scores in order of highest to lowest', () => {});
});
describe('Descending order (Test C)', () => {
  it('Scenario: User navigates to leaderboard, expect: a table of users and scores in order of highest to lowest based on accuracy', () => {});
});
